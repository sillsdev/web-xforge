import { InsightCodePipe } from './insight-code.pipe';

describe('InsightCodePipe', () => {
  it('create an instance', () => {
    const pipe = new InsightCodePipe();
    expect(pipe).toBeTruthy();
  });
});
