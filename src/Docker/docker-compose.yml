services:
  db-xforge:
    image: mongo:5.0

  realtimeserver:
    image: ${DOCKER_REGISTRY-}sillsdev/web-xforge/realtimeserver
    build:
      context: ..
      dockerfile: ./RealtimeServer/Dockerfile

  web-xforge:
    image: ${DOCKER_REGISTRY-}sillsdev/web-xforge
    build:
      context: ..
      dockerfile: ./SIL.XForge.Scripture/Dockerfile

volumes:
  mongo_config:
  mongo_data:
  scriptureforge-data:
  xforge-data:
